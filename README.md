# Extractor
Extractor is a QGIS Plugin that allows you to extract pixel values from a single or multiple raster layer and save it as a CSV or Excel file. Additionally, it performs zonal statistics on a vector layer and raster layer, calculating statistics such as mean, sum, max, and min for each zone.
